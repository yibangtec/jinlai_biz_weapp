<!--create_result.wxml-->
<text>{{title}}</text>
<button class="verify-code clearfix" class="bnt-left" type="primary" size="mini" loading="{{loading}}" plain="{{defaultPlain}}"
        disabled="{{disableVer}}" bindtap="backIndex"> 首页 </button>
        <button class="verify-code clearfix" class="bnt-left" type="primary" size="mini" loading="{{loading}}" plain="{{defaultPlain}}"
        disabled="{{disableVer}}" bindtap="backMine"> 我的 </button>
